# Backend SaaS — single source of truth (PostgreSQL/SQLite), no Sheets
python-dotenv>=1.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth (JWT + bcrypt PIN per business)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.14.0

# Chatbot runtime deps
Flask>=3.0.0
twilio>=9.0.0
requests>=2.31.0
rapidfuzz>=3.0.0
waitress>=3.0.0

# Cache / pub-sub (Redis — for WS fanout & stateless multi-worker)
redis>=5.0.0

# Push notifications (optional)
firebase-admin>=6.5.0

# Dev / validation
httpx>=0.27.0
pytest>=8.0.0
